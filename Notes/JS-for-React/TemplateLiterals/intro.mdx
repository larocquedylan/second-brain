# Template Literals

Template literals are like strings but allow for things like multi-line string, string interpolation with embedded javascript expressions, and special constructs.

```js
// string
`string`// multi-line string
`string 1 
and string 2`// string interpolation
`string where we can evaluate   1 + 1  = ${1 + 1} with a '$' plus "{}" `;
```
